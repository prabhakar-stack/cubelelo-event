/**
 * This file is kept as a stub so old imports don't break during migration.
 * The project has migrated from Prisma (PostgreSQL) to Mongoose (MongoDB).
 * All database logic now lives in:
 *   - lib/mongodb.ts   — native MongoClient for NextAuth adapter
 *   - lib/mongoose.ts  — Mongoose connection helper
 *   - lib/models/      — Mongoose data models
 *
 * You can delete this file once all imports have been updated.
 */

export const prisma = null;
